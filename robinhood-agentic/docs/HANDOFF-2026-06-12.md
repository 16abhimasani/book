# Handoff — strategy-optimization session, 2026-06-12

For the reviewing agent (and owner). This session produced
`docs/STRATEGY-REVIEW-2026-06-11.md` (in this folder), two data
scaffolds, and a JOURNAL entry — all on branch
`claude/trading-strategy-optimization-kxgc8q`. The session was
**read-only against the broker**: no orders placed, no orders
modified, no POLICY.md edits. Everything below is context that isn't
obvious from the diff itself.

## What to verify before merging

1. **JOURNAL.md merge conflict risk (act soon).** This branch appends
   a journal entry. The hourly heartbeat appends to `main`. The longer
   this PR sits, the more likely an append-conflict at the bottom of
   JOURNAL.md. Resolution is trivial (keep both entries, chronological
   order) but do it promptly — or cherry-pick the journal entry to
   main first if review will take days.
2. **`data/marks.csv` 2026-06-11 closes are last regular-session
   prints (19:59:59 UTC), not official settled SIP closes.** QQQ
   716.44 / VIXY 24.42. The official close can differ by cents. Fine
   for a 20-day MA gate; if the EOD routine later writes official
   closes, expect tiny discrepancies with this seed row. The
   2026-06-10 row IS official closes (MCP `closes` field).
3. **`data/trades.csv` risk_pct_acct uses the $3,000 entry-time
   account value**, not current value. R-multiple on close should use
   `risk_usd` as the denominator (that's the point of recording it).
4. **The "143% beta-adjusted exposure" figure** in the review was
   computed at 2026-06-11 marks (MU 995.65 + INTC 701.94 + TQQQ
   912×3 = $4,434 / $3,098). It moves with prices; recompute before
   quoting it elsewhere.

## Decisions queued for the owner (priority order, my recommendation)

The review proposes 9 POLICY v0.2 diffs. If adopting incrementally:

1. **Diff 5 (Lane-1 exit ladder / breakeven ratchet)** — highest
   urgency: MU was +5.75% at review time with a stop still −8% below
   entry. NOTE: the hourly loop can already apply this TODAY without
   any POLICY edit — POLICY §0 says "tighter-than-policy judgment is
   always allowed". Raising MU's stop to breakeven (replace GTC order
   6a2af328) is pure tightening. The journal entry flags this for the
   next hourly run.
2. **Diff 4 (settled-funds / GFV rule)** — zero cost, prevents the
   one failure mode (90-day restriction) that ends the experiment
   regardless of P&L. Cash account + hourly recycling is exactly the
   GFV pattern.
3. **Diffs 1–3 (risk budget, beta-adjusted cap, theme cap)** — the
   core structural fix; affects the NEXT entries, so adopt before the
   current positions close and capital redeploys.
4. **Diffs 7–8 (entry hygiene, marks.csv EOD step)** — codify what's
   already practiced.
5. **Diffs 6, 9** — with the rest.

## Maintenance contracts created by this PR (need wiring)

The two CSVs only have value if the heartbeat maintains them. The
OPERATIONS §B routine prompt template does NOT yet mention them.
After merge, the routine prompt (and/or POLICY §4–5 via diff 8)
needs two lines added:

- EOD run: append `date,qqq_close,vixy_close,account_value` to
  `robinhood-agentic/data/marks.csv` (use MCP official closes).
- Any run that closes a round-trip: fill the row in
  `robinhood-agentic/data/trades.csv` (exit_date, exit_price, pnl_usd,
  r_multiple = pnl_usd / risk_usd) and add rows for new entries.

Until that wiring exists, the files will silently go stale — worse
than not existing, because the regime gate would read an old MA.

## Findings that did NOT make the review doc

- **MCP tool surface checked 2026-06-11 ~22:20 UTC:** equities +
  watchlist only, as POLICY assumes. No options/crypto/event-contract
  tools yet (Lane 4 stays parked). `get_accounts` confirms the
  Agentic account `••••5686` / `786675686`, `agentic_allowed: true`,
  cash, options level 2.
- **VIX index is not quotable** via `get_equity_quotes` (verified —
  request for VIX returned only the ETFs). VIXY, VXX, UVXY, SVIX all
  quote fine. VIXY chosen for marks.csv as the plainest 1x short-term
  futures proxy. Caveat for the gate: VIX-futures ETFs decay, so
  **never use an absolute VIXY level as the "VIX < 25" leg** — use
  day-over-day / vs-own-average direction only, or keep estimating
  spot VIX from news until a real feed exists.
- **Extended-hours probe idea** (review §4): order history shows
  `market_hours: regular_hours` on all orders; if
  `review_equity_order` accepts an extended-hours flag, the 8:30 ET
  pre-market run could place real catalyst entries instead of just
  planning. Suggested method: probe with `review_equity_order` only
  (never place), journal the result, owner decides. Nobody has run
  this probe yet.
- **One-heartbeat warning still live:** OPERATIONS §B warns the local
  Cowork task `rh-trading-loop-local` must be disabled once the cloud
  routine journals cleanly. This session could not verify which
  heartbeat(s) are currently active — journal entries up to #2 came
  from the local task. Reviewer: confirm before both are running.
- **Stop order IDs at handoff** (for cross-checking later journal
  entries): MU 866 GTC = `6a2af328`, INTC 105 GTC = `6a2af32a`,
  TQQQ 63.20 GTC = `6a2af57f`. All `confirmed`, regular-hours,
  stop-market, placed_agent=agentic.

## What this session deliberately did not do

- No POLICY.md edits (owner-only per §0) — that's why the diffs live
  in the review doc.
- No orders, even allowed tightenings — the session brief was
  strategy research; order management belongs to the heartbeat, and
  two writers on one book is the failure mode OPERATIONS §B warns
  about.
- No OPERATIONS.md edits — the routine-prompt wiring (above) touches
  the live heartbeat's instructions; safer done by the owner/main
  agent together with the POLICY version bump so prompt and policy
  stay in sync.
- No backtesting harness in `src/` — still parked per
  BRAINSTORM §Parked; trades.csv expectancy math doesn't need it.

## Session metadata

- Date: 2026-06-12 (UTC); market data observed 2026-06-11 ~22:20 UTC
  (after-hours).
- Branch: `claude/trading-strategy-optimization-kxgc8q`, based on
  `main` @ 57b1980.
- Account snapshot at observation: $3,097.89 total / $481.40 cash /
  3 positions (MU, INTC, TQQQ), 3 GTC stops working, no other open
  orders.
